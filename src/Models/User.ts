export type Id = number | string;

export type UserProfileToken = {
    name: string,
    email: string
    token: string
}

export type UserProfile = {
    id: number,
    name: string,
    email: string,
    avatar: string
}

